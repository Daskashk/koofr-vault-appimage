[dependencies]
tauri = { version = "1.8.0", features = ["shell-open"] } # Downgrade from "2.0.0"
# Also ensure tauri-build uses a compatible 1.x version
[build-dependencies]
tauri-build = { version = "1.5.0" }
